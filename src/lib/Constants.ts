// Configuration constants

// The display name of the website
export const SITE_NAME = "Dailies";

// URL for the API endpoint
export const API_ENDPOINT_URL = "./api";

// The minimum content length at which warning highlights will be shown
export const ENTRY_CONTENT_WARN_LENGTH = 180;

// When fetching an entry from the server, determine how many extra entries 
// before and after the requested entry to pre-fetch.
export const NEARBY_ENTRIES_TO_PREFETCH = 0;
